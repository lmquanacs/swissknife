In `evals/fixtures/inventory`, I want the retry behaviour made configurable per
endpoint instead of globally. Don't write any code yet — tell me what you'd need
to read first and what you found. Keep the reading tight; this is a small job in
a small repo.
