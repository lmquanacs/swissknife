I'm handing `evals/fixtures/shop` to another engineer who has never seen it.
Write them a context pack for the task "make the retry behaviour configurable
per endpoint". They should be able to start without opening anything else.
